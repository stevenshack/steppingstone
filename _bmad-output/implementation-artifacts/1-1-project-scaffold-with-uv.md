# Story 1.1: Project scaffold with uv

Status: done

## Story

As a developer,
I want the project to have a working Python environment with uv and the defined directory structure,
so that I can clone the repo, run `uv sync`, and start building pipeline components immediately.

## Acceptance Criteria

1. **Given** the project directory **When** I run `uv sync` **Then** a virtual environment is created with all declared dependencies installed
2. **Given** the project structure **When** I list the root directory **Then** I see directories: `app2source/`, `nextstep2gnustep/`, `steps/`, `lib/`, `clang-ast-fixup/`, `tests/`
3. **Given** the scaffolded project **When** I run `uv run app2source --help` **Then** it prints usage information and exits with code 0
4. **Given** the scaffolded project **When** I run `uv run nextstep2gnustep --help` **Then** it prints usage information and exits with code 0

## Tasks / Subtasks

- [x] Task 1: Create pyproject.toml with uv configuration (AC: 1)
  - [x] Declare Python dependency (>=3.10) and project metadata
  - [x] Define `[project.scripts]` entry points: `app2source` and `nextstep2gnustep`
  - [x] Include dev dependencies: pytest, and any others as needed
  - [x] Run `uv sync` to generate uv.lock
- [x] Task 2: Create directory structure (AC: 2)
  - [x] Create `app2source/` package (__init__.py, __main__.py, orchestrator.py)
  - [x] Create `nextstep2gnustep/` package (__init__.py, __main__.py, orchestrator.py)
  - [x] Create `steps/` package with __init__.py
  - [x] Create `lib/` package with __init__.py
  - [x] Create `clang-ast-fixup/` directory with CMakeLists.txt stub, src/, include/fixup/
  - [x] Verify `tests/` already exists; add __init__.py if missing
- [x] Task 3: Implement app2source CLI entry point (AC: 3)
  - [x] `app2source/__main__.py` — argparse-based CLI that prints usage and exits
  - [x] `app2source/orchestrator.py` — stub that will compose pipeline steps in later stories
- [x] Task 4: Implement nextstep2gnustep CLI entry point (AC: 4)
  - [x] `nextstep2gnustep/__main__.py` — argparse-based CLI that prints usage and exits
  - [x] `nextstep2gnustep/orchestrator.py` — stub that will compose translation steps in later stories
- [x] Task 5: Create top-level Makefile (AC: all)
  - [x] Targets: `install` (uv sync), `test` (pytest), `clean`
- [x] Task 6: Verify all ACs pass

## Dev Notes

### Architecture Compliance

Source: `_bmad-output/planning-artifacts/architecture.md`

- **Language stack:** Python-primary; Clang AST fixup is C++ subprocess (not in this story; scaffold only)
- **Toolchain:** `uv` for project management — `pyproject.toml` is the single source of truth
- **Naming conventions (PEP 8):** `snake_case` for functions/variables, `CamelCase` for classes, `SCREAMING_SNAKE` for constants, `_leading_underscore` for private members
- **Entry point naming:** Verb-based — `app2source`, `nextstep2gnustep`
- **Subprocess protocol (future):** Tools communicate via stdout (JSON result), stderr (progress), exit code. Not used in this story, but the orchestrator stubs should be structured for it.

### File Structure Requirements

```
steppingstone/
├── pyproject.toml              # uv project config — CREATE
├── uv.lock                     # Lockfile — GENERATED by uv sync
├── Makefile                    # Top-level: install, test, clean — CREATE
├── app2source/                 # Phase 1 orchestrator — CREATE
│   ├── __init__.py
│   ├── __main__.py             # CLI entry point: uv run app2source
│   └── orchestrator.py         # Phase 1 pipeline composition (stub)
├── nextstep2gnustep/           # Phase 2 orchestrator — CREATE
│   ├── __init__.py
│   ├── __main__.py             # CLI entry point: uv run nextstep2gnustep
│   └── orchestrator.py         # Phase 2 pipeline composition (stub)
├── steps/                      # Individual pipeline tools — CREATE
│   └── __init__.py
├── lib/                        # Shared utilities — CREATE
│   └── __init__.py
├── clang-ast-fixup/            # C++ Clang/LLVM subprocess tool — CREATE skeleton
│   ├── CMakeLists.txt          # Stub CMake build
│   ├── src/                    # (empty for now)
│   └── include/
│       └── fixup/              # (empty for now)
└── tests/                      # Test directory — EXISTS, preserve content
```

### Existing Files to Preserve

The repository already contains working scripts and analysis tools in the root directory. Do NOT delete or move any of the following:
- `*.py` scripts in root (dump_objc_metadata.py, extract_nib.py, nib_parse.py, etc.)
- `*.sh` scripts (analyze_app.sh, pipeline.sh, etc.)
- `*.m` / `*.c` source files
- `patches/`, `ghidra_scripts/`, `docs/` directories
- `tests/` directory and all its contents
- `README.md`, `STATUS.md`

The new directories and files are additive — they establish the architecture-defined structure alongside the existing work.

### Technical Requirements

- **Python version:** >=3.10
- **Build system:** `pyproject.toml` with setuptools or hatchling backend (uv-compatible)
- **CLI framework:** `argparse` (stdlib, no extra dependency)
- **Entry points format in pyproject.toml:**
  ```toml
  [project.scripts]
  app2source = "app2source.__main__:main"
  nextstep2gnustep = "nextstep2gnustep.__main__:main"
  ```
- **`__main__.py` pattern:** Define a `main()` function, guarded by `if __name__ == "__main__": main()`
- **Orchestrator stubs:** `orchestrator.py` in each package defines a `run()` function that takes paths as arguments. Bodies can be `raise NotImplementedError("Phase 1 pipeline not yet implemented")` — they exist as contracts for later stories.
- **clang-ast-fixup/CMakeLists.txt:** Minimal stub — `cmake_minimum_required(VERSION 3.16)` and `project(clang-ast-fixup LANGUAGES CXX)` with C++17 standard. No sources to compile yet.

### Makefile Targets

Source: architecture.md `## Project Structure & Boundaries`

```makefile
.PHONY: install test clean

install:
	uv sync

test:
	uv run pytest

clean:
	rm -rf __pycache__ .pytest_cache
	find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
```

### Testing Requirements

- No unit tests required for scaffolding story — ACs are verified by running commands
- The existing `tests/` directory should remain untouched
- Future stories will add `pytest`-based unit tests in `tests/`

### References

- [Source: _bmad-output/planning-artifacts/epics.md — Epic 1, Story 1.1]
- [Source: _bmad-output/planning-artifacts/architecture.md — Project Structure & Boundaries, Implementation Patterns]
- [Source: _bmad-output/planning-artifacts/prd.md — Implementation Considerations]
- [Source: _bmad-output/planning-artifacts/implementation-readiness-report-2026-05-12.md — Next Steps: Story 1.1 first]

## Dev Agent Record

### Agent Model Used

deepseek-v4-pro

### Debug Log References

### Completion Notes List

- ✅ Created `pyproject.toml` with setuptools backend, Python >=3.10, entry points for `app2source` and `nextstep2gnustep`, and dev dependency group with pytest
- ✅ Ran `uv sync --dev` successfully — virtual environment created, uv.lock generated
- ✅ Created directory structure: `app2source/`, `nextstep2gnustep/`, `steps/`, `lib/`, `clang-ast-fixup/`
- ✅ Implemented `app2source/__main__.py` argparse CLI — `uv run app2source --help` prints usage, exits 0
- ✅ Implemented `app2source/orchestrator.py` with `run()` stub raising `NotImplementedError`
- ✅ Implemented `nextstep2gnustep/__main__.py` argparse CLI — `uv run nextstep2gnustep --help` prints usage, exits 0
- ✅ Implemented `nextstep2gnustep/orchestrator.py` with `run()` stub raising `NotImplementedError`
- ✅ Created `clang-ast-fixup/CMakeLists.txt` stub (C++17, cmake 3.16)
- ✅ Created top-level `Makefile` with `install`, `test`, `clean` targets
- ✅ All 17 existing tests in `tests/` pass with no regressions

### File List

- `pyproject.toml` — new
- `uv.lock` — new (generated)
- `Makefile` — new
- `app2source/__init__.py` — new
- `app2source/__main__.py` — new
- `app2source/orchestrator.py` — new
- `nextstep2gnustep/__init__.py` — new
- `nextstep2gnustep/__main__.py` — new
- `nextstep2gnustep/orchestrator.py` — new
- `steps/__init__.py` — new
- `lib/__init__.py` — new
- `clang-ast-fixup/CMakeLists.txt` — new

### Change Log

- Scaffolded project with uv, directory structure, CLI stubs, and Makefile (2026-05-12)

### Review Findings

- [x] [Review][Decision] Inconsistent default output directories — `app2source` default changed from `.` to `app2source-output`. Resolved: intentional per-phase naming.

- [x] [Review][Decision] Makefile `install` uses `uv sync --dev` vs spec's `uv sync` — Confirmed intentional: dev deps needed for `make test`.

- [x] [Review][Patch] `sys.exit(0)` on missing positional argument masks failure — Fixed: changed to `sys.exit(2)` in both CLIs. [`app2source/__main__.py:26`, `nextstep2gnustep/__main__.py:26`]

- [x] [Review][Patch] `find -exec rm -rf {} +` lacks `-depth` flag — Fixed: added `-depth` to Makefile clean target. [`Makefile:11`]

- [x] [Review][Defer] CLI path arguments not validated — Positional args (`macho`, `source_dir`) and `--output-dir` are not checked for existence, readability, or writability. Deferred — `run()` is a stub that raises `NotImplementedError`, so issues won't surface until pipeline implementation. [`app2source/__main__.py`, `nextstep2gnustep/__main__.py`]
